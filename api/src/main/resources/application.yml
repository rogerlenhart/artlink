spring:
  datasource:
    username: postgres
    password: 123456
  jpa:
    hibernate:
       ddl-auto: validate
server:
  error:
    include-message: always
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
